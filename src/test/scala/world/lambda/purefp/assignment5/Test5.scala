package world.lambda.purefp.assignment5

import org.specs2.mutable.Specification

class Test5 extends Specification {
  "Try to write a test which has the same semantic of Test4 but without using mocks" >> {
      success
  }
}
